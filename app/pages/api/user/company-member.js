import User from '../../../models/user'

export default async function handler(req, res) {
    console.log('[create new team member]: Handler start');

    try {
        
    } catch (error) {
        res.status(500).send(error);
    }
}